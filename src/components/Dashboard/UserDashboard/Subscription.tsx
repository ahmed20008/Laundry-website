import React from 'react';

interface Props {
  // Props go here
}

const Subscription: React.FC<Props> = (props) => {
  return (
    <div>
      <p>Subscription</p>
    </div>
  );
};

export default Subscription;