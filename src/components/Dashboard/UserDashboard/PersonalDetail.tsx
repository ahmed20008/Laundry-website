import React from 'react';

interface Props {
  // Props go here
}

const PersonalDetail: React.FC<Props> = (props) => {
  return (
    <div>
      <p>PersonalDetail</p>
    </div>
  );
};

export default PersonalDetail;